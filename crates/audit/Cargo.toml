[package]
name = "andna-audit"
version = "0.1.0"
edition = "2021"

[dependencies]
andna-contracts = { path = "../contracts" }
sha2 = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha3 = "0.10"

[features]
default = []
oqs-backend = []
stub = []
